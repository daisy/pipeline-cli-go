 * Better error reporting, now too techie
 * FIXME in scripts.go
 * Make sure that all the config items are correctly propagated (timeout is now ignored)
 * Fix help so we get the flags always in the same order
